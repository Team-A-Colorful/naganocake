<div class="col-2">
  <table class="table table-bordered table-hover mt-5">
    <tr class="table-active text-center justify-content-center">
      <th>ジャンル検索</th>
    </tr>
    <% genres.each do |genre| %>
    <tr class="tr-color ">
      <td class="text-center"><%= link_to "#{genre.name}", genre_search_path(id: genre.id),class: "text-dark"%></td>
    </tr>
    <%end%>
  </table>
</div>
 