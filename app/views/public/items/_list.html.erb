<div class="col-md-3">
  <div class="card mt-5">
    <h6 class="card-header">ジャンル検索</h6>
    <div class="card-body">
      <% genres.each do |genre| %>
        <p class="card-text"><%= genre.name %></p><br>
      <% end %>
    </div>
  </div>
</div>